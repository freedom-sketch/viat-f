2. Рекурсия - это определение объекта посредством ссылки на себя.
3. 1) Вы подносите микрофон к колонкам, произносите в микрофон стартовое слово и оно звуча из колонок снова попадает в микрофон и звук начинает рекурсивно распространяться
   2) Классический резонанс может рассматриваться и как рекурсивное наложение колебаний друг на друга с эффектом сумарного накопления амплитуды
   3) Чтобы рыба могла жить она движется для того, чтобы вода омывала жабры
   4) круговорот воды в природе
4.  это алгоритм, в определении которого содержится прямой или косвенный вызов этого же алгоритма
5. Рекурсивная триада:
 1) параметризация
 2) база рекурсии
 3) декомпозиция
6. Полное дерево - граф, который визуализирует все вызовы рекурсивной функции для оценки трудоемкости алгоритма
   Глубина рекурсии - наибольшее одновременное количество рекурсивных обращений функции, определяющее максимальное количество слоев рекурсивного стека, в котором осуществляется хранение отложенных вычислений
   Объем рекурсии -  количество вершин полного рекурсивного дерева без единицы
7. Рекурсивный стек:
   Для каждого текущего обращения формируется локальный
слой данных стека
   Завершение вычислений происходит посредством
восстановления значений данных каждого слоя в порядке,
обратном рекурсивным обращениям
8.
function factorial(n: integer): integer;   <--- Параметризация
begin           __
  if n = 1 then   |
  begin           |
    Result := 1;  | <--- База рекурсии
    exit;         |
  end;          __|

  Result := n * factorial(n-1);   <--- Декомпозиция
end;
Дерево: фото в папке
Глубина: n-1
Объем: n-2
9. Фото в папке. Некий врач выполняет процедуру узи на матрешке и на экране видит матрешку
